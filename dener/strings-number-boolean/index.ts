// let pedir = "pizza"
// pedir = 11 não funciona pois ele inicia com tipo string

let pedir: string = "pizza"
// pedir = 11 não funciona pois está claro que só se aceita string

function falar(frase: string): boolean {
  console.log(frase);
  return true
}